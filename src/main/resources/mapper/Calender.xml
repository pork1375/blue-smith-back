<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.blueSmith.calender.mapper.CalenderMapper">
    <select id="getCalender" resultType="com.example.blueSmith.calender.dto.CalenderDto">
        select
        id
        ,startDate
        ,endDate
        from calender
    </select>
</mapper>